package main

import (
	"strings"
	"container/list"
)

func ProcessLine(line string)(*list.List){
	processedLine := strings.Fields(line)
	for i, v := range processedLine{
		
	}
	var lineFields = list.New()
	return lineFields
}